import math
num = float(input('Digite um valor: '))
print('O valor digitado foi {} e a sua porção inteira é {}.'.format(num, math.trunc(num)))

# Crie um programa que leia um número real qualquer pelo teclado e mostre na tela a sua porção inteira.

# Importar a biblioteca de operações matemáticas (math) para poder usar o método trunc na variável num.
# Método trunc (truncate) para cortar a parte inteira do número.
# Se eu importar todas as funções matemáticas, devo usar math.método(e a variável a ser aplicado o método).
# No ex024, farei o mesmo programa, porém importando apenas a função desejada.

