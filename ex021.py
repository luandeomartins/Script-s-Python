from math import sqrt, floor
num = int(input('Digite um número: '))
raiz = sqrt(num)
print('A raiz de {} é igual à {}.'.format(num, floor(raiz)))
# Explicação e exemplo da importação somente da biblioteca que vou usar.